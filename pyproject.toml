[project]
name = "elevenlang-exp"
version = "0.1.0"
description = "Public-safe demo repo for 11/11 Language (interfaces + stub pipeline)"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
elevenlang = "elevenlang.cli:main"

[tool.pytest.ini_options]
testpaths = ["tests"]
